// it sets a prefix for all constants to avoid conflicts
export const PREFIX: string = "@reactLang/";
// it is used to launch an action for setting current language
export const SET_LANG: string = PREFIX + "SET_LANG";
// it is used to launch an action for setting current messages
export const SET_MESSAGES: string = PREFIX + "SET_MESSAGES";
